DB = Creacion de las tablas (incidents/ticket){
    clave foranea entre incidents y ticket
}

Model = Declaracion de la estructura de los objetos (inci./tick.){
    incident( id,
    desciption,
    type)

    ticket(id,
    client,
    service,
    incident,
    status,
    creation_date
    closing_date)
}

Controller = intermediario entre el modelo (DB/Module/repositories) y la vista (Tkinter)




DB --> repositories.py -----> Controller ---> Tkinter
